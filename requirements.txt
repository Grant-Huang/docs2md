# 文档资料知识化APP - Docx + Excel 转换
fastapi>=0.109.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-docx>=1.0.0
markitdown>=0.0.1
openai>=1.0.0
aiofiles>=23.0.0

# MCP 服务器（Claude 桌面版 / 任意 MCP 客户端集成）
mcp[cli]>=1.0.0

# 旧版格式升级（.doc → .docx，.xls → .xlsx）
# Linux/Mac：需安装系统包 LibreOffice（sudo apt install libreoffice 或 brew install libreoffice）
# Windows：优先使用 LibreOffice，不可用时退化为 win32com（需安装 Microsoft Office）
pywin32>=306; sys_platform == "win32"
